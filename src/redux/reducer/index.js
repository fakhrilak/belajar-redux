import { combineReducers } from "redux";


import home from "./home"
///dibawah ini tambahin state dan combain di reducer contoh : combineReducers({home,"nama statenya"})

export default combineReducers({home});